# CMake minimun version required.
cmake_minimum_required(VERSION 3.7)

# Set the project's name.
project(
  CPGE
  VERSION 0.0.0.1
  LANGUAGES CXX)

# Add the library subdurectory.
add_subdirectory(lib)
